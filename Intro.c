// need in order to use the program
#include <stdio.h>
#include <stdlib.h>

// main method 
int main()
{
    // variables
    // letters 
    char characterName[] = "Keely";
    char grade = 'A';
    // numbers
    int characterAge = 35;
    double gpa = 4.0;

    // print statement
    printf("There once was a girl named %s \n", characterName);
    printf("She was %d years old \n", characterAge);
    printf("%f", 4.5); 

    return 0;
}